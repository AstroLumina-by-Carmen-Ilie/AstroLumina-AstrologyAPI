services:
  traefik:
    image: traefik:v3.1
    container_name: astrology-api-traefik
    restart: unless-stopped
    volumes:
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic:/etc/traefik/dynamic:ro
      - ./certs:/certs
      - ./logs:/var/log/traefik
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    networks:
      - astrology-api

  astrology-api-blue:
    image: ghcr.io/astrolumina-by-carmen-ilie/astrolumina-astrologyapi:2.0.1
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3031
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://localhost:3031/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.125'
          memory: 128M
    networks:
      - astrology-api

  astrology-api-green:
    image: ghcr.io/astrolumina-by-carmen-ilie/astrolumina-astrologyapi:2.0.1
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3031
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://localhost:3031/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.125'
          memory: 128M
    networks:
      - astrology-api

networks:
  astrology-api:
    name: astrology-api-network
    driver: bridge
